---
title: Self-Attention and Transformers
lecture: 8
video: https://www.youtube.com/watch?v=LWMzyfvuehA
source: edited from auto-generated YouTube transcript
---

# Self-Attention and Transformers — transcript

Copy-edited for punctuation, sentence boundaries, and mis-heard terms; cross-checked
against `raw/slides/08-self-attention-and-transformers.md`. Mathematical expressions
dictated aloud are written in symbols (bold for vectors/matrices, Unicode subscripts),
matching the convention used for lecture 3. The verbatim auto-generated captions are
kept at `raw/transcripts/original/08-self-attention-and-transformers.md`. Lecturer is
Anna Goldie (slides adapted from Anna Goldie and John Hewitt). Student questions and
comments from the floor are set in *italics*. Timestamps mark the start of each
paragraph.

**[0:04]** Hi everyone, welcome to CS224N — we're about two minutes in, so let's get started. Today we've got what I think is quite an exciting lecture topic: we're going to talk about self-attention and Transformers. These are some ideas that are the foundation of most of the modern advances in natural language processing, and actually of AI systems in a broad range of fields, so it's a very fun topic. Before we get into that — before we get into that — we're going to have a couple of reminders: there are brand new lecture notes.

**[0:52]** I'm very excited about them — they pretty much follow along with what I'll be talking about today, but go into considerably more detail. Assignment 4 is due a week from today. The issues with Azure continue — thankfully our TAs especially have tested that this works on Colab, and the amount of training is such that a Colab session will allow you to train your machine translation system, so if you don't have a GPU, use Colab. We're continuing to work on getting access to more GPUs for Assignment

**[1:40]** 5 and the final project — we'll continue to update you as we're able to, but our usual systems this year are no longer holding, because companies are changing their minds about things. Our final project proposal — you'll have a proposal of what you want to work on for your final project, and we will give you feedback on whether we think it's a feasible idea, or how to change it, so this is very important, because we want you to work on something that has a good chance of success for the rest of the quarter. That's going to be out tonight — we'll have an Ed announcement when it is out — and we want to get you feedback on that pretty quickly, because you'll be working on this after Assignment 5 is done. Really, the major core component of the course after

**[2:26]** that is the final project. *[Instructor: any questions? Cool.]* So let's take a look back at what we've done so far in this course, and see what our strategy was: if you had a natural language processing problem and you wanted to take your best-effort attempt at it, without doing anything too fancy, you would have said, "Okay, I'm going to have a bidirectional LSTM instead of a simple RNN — I'm going to use an LSTM to encode my sentences, I get bidirectional context — and if I have an output I'm trying to generate, I'll have a unidirectional LSTM that I was

**[3:13]** going to generate one word at a time." So you have a translation, or a parse, or whatever, and maybe I've encoded the source sentence in a bidirectional LSTM, and I'm one-by-one decoding out the target with my unidirectional LSTM, and then also I was going to use something like attention to give flexible access to memory, if I felt like I needed to look back and see where I want to translate from. This was working exceptionally well — we motivated attention through wanting to do machine translation, and you have this bottleneck where you don't want to have to encode the whole source sentence into a single vector. In this lecture we have the same goal: we're going to be looking at a lot of the same problems we did previously, but we're going to use

**[3:59]** different building blocks. From about 2014 to 2017-ish we were using recurrence, and through lots of trial and error, years later we had these brand new building blocks we could plug in as a direct replacement for LSTMs, and they're going to allow for a huge range of much more successful applications. So what are the issues with the recurrent neural networks we used to use, and what are the new systems we're going to use, from this point moving forward? One of the issues with a recurrent neural network is what we're going to call linear interaction distance. As we know, RNNs are unrolled left to right, or right

**[4:46]** to left, depending on the language and the direction, and it encodes a notion of linear locality, which is useful, because if two words occur right next to each other, sometimes they're actually quite related — "tasty pizza," they're nearby, and in the recurrent neural network you encode "tasty," and then you walk one step and encode "pizza." So nearby words do often affect each other's meanings, but you have this problem where very long-distance dependencies can take a very long time to interact. If I have the sentence "the chef" — those are nearby, they interact with each other — and then "who," and then a bunch of stuff, like "the chef who went to the stores and picked up the ingredients and

**[5:32]** loves garlic and then was" — right, I actually have an RNN step, this application of the recurrent weight matrix and some element-wise nonlinearities, once, twice, three times, as many times as there is potentially the length of the sequence between "chef" and "was." "It's the chef who … was" — this is a long-distance dependency, and it should feel kind of related to the stuff we did in dependency syntax, but it's quite difficult to learn that these words should be related if you have a lot of steps between words —

**[6:18]** it can be difficult to learn the dependencies between them. We talked about all these gradient problems — LSTMs do a lot better at modeling gradients across long distances than simple recurrent neural networks, but it's not perfect, and we already know this linear order isn't really the right way to think about sentences. If I wanted to learn that it's "the chef who … was," I might have a hard time doing it, because the gradients have to propagate from "was" to "chef," and I'd really like a more direct connection between words that might be related in the sentence, or in a document even, if these are going to get much longer.

**[7:03]** So this is the linear interaction distance problem: we would like words that might be related to be able to interact with each other in the neural network's computation graph more easily than by being linearly far away, so that we can learn these long-distance dependencies better. There's a related problem too, that again comes back to the recurrent neural network's dependence on the index into the sequence — often called a dependence on time. In a recurrent neural network the forward and backward passes have O(sequence length) many — roughly sequence-length-many — unparallelizable operations, so GPUs are great, they can do a lot of operations at once, as long as

**[7:51]** there's no dependency between the operations in terms of time, where you have to compute one and then compute the other. But in a recurrent neural network you can't actually compute the RNN hidden state for time step 5 before you compute the hidden state for time step 4, or time step 3. You get a graph that looks like this: if I want to compute this hidden state, for the first word I have zero operations I need to do before I can compute that state; I have one operation I can do before I can compute the next state; and as my sequence length grows, here I've got three operations I need to do before I can compute the state at index three, because I need to compute this and this and that — so there are three unparallelizable operations that I'm

**[8:37]** glomming — all the matrix multiplies and stuff — into a single one. So: one, two, three, and of course this grows with the sequence length as well. So as the sequence length grows I can't parallelize — I can't just have a big GPU do one matrix multiply to compute this state, because I need to compute all the previous states beforehand. *[Student question:]* "I have a question on the linear interaction issue — I thought that was the whole point of the attention network. Can't we do something like attention to sort of work our

**[9:25]** way [around the parallelizability problem too]?" So the question is, with the linear interaction distance — wasn't this sort of the point of attention, that it gets around that? Can't we use something with attention to help? It won't solve the parallelizability problem — and in fact, everything we do in the rest of this lecture will be attention-based, but we'll get rid of the recurrence and just do attention, more or less. Good intuition. Any other questions? Okay, cool. So, if not recurrence, what about attention — just a slide back. We're going to get deep into attention today, but just for a second: attention treats each word's representation as a query, to access and incorporate information from

**[10:11]** a set of values. Previously, we were in a decoder, decoding out a translation of a sentence, and we attended to the encoder, so that we didn't have to store the entire representation of the source sentence into a single vector. Today we'll think about attention within a single sentence — I've got a sentence written out here, word one through word *t*, and on these integers in the boxes I'm writing the number of unparallelizable operations you need to do before you can compute these. For each word, you can independently compute its embedding without doing anything else, because the embedding just depends on the word identity, and then with attention, if I wanted to build an attention representation of a word by looking at all the other words in the sequence,

**[10:57]** that's one big operation, and I can do it in parallel for all the words — the attention for this word, the attention for that word — I don't need to walk left to right like I did for an RNN. We'll get much deeper into this, but you should have the intuition that it solves the linear interaction problem and the non-parallelizability problem, because now, no matter how far away words are from each other, I'm potentially interacting — I might attend to you even if you're very far away, independent of how far away you are — and I also don't need to walk along the sequence linearly. I'm treating the whole sequence at once. So the intuition is that attention allows you to look very far away at once, and it doesn't

**[11:43]** have this dependence on the sequence index that keeps us from parallelizing operations. So now, the rest of the lecture, we'll talk in great depth about attention — let's move on. Let's think more deeply about attention. One thing you might think of with attention is that it's performing a kind of fuzzy lookup in a key-value store — you have a bunch of keys, a bunch of values, and it helps you access that. In an actual lookup table — just like a dictionary in Python, for example, very simple — you have a table of keys, each key maps to a value, and you give it a query, and the query matches one of the keys, and then you return the value. I've got a bunch of keys here,

**[12:31]** and my query matches a key, so I return the value — simple, fair, easy. And in attention, just like we saw before, the query matches all keys softly — there's no exact match, you compute some similarity between the query and all of the keys, and then you weight the results. You've got a query, you've got a bunch of keys, the query to different extents is similar to each of the keys, and you measure that similarity between zero and one through a softmax, and then you get the values out — you average them via the weights,

**[13:16]** the similarity between the query and the keys — you do a weighted sum with those weights, and you get an output. So it really is quite a bit like a lookup table, but in this soft, vector-space, mushy sort of sense — I'm accessing information stored in the key-value store, but I'm softly looking at all of the results. Any questions there? Cool. So what might this look like — if I was trying to represent the sentence "I went to Stanford's CS224N and learned," and I'm trying to build a representation of "learned": I have a key for each word — this is the self-attention

**[14:03]** thing we'll get into — I have a key for each word, a value for each word, I've got the query for "learned," and I've got these teal-ish bars up top, which might say how much you're going to try to access each of the words — so maybe "224N" is not that important, "CS" maybe determines what I learned, "Stanford" — and then "learned" — maybe that's important to representing itself. So you look across the whole sentence and build up this soft accessing of information across the sentence, in order to represent "learned" in context. This is just a toy diagram — let's get into the math. We're going to look at a sequence of words, **w**₁ to **w**ₙ, a sequence of words in a vocabulary — like, you know, "Zuko made his uncle tea," that's a good

**[14:49]** sequence — and for each word we're going to embed it with an embedding matrix, just like we've been doing in this class. I have this embedding matrix, **E**, that goes from the vocabulary size to the dimensionality *d*, so each word has a non-contextual — only dependent on itself — word embedding, and now I'm going to transform each word with one of three different weight matrices. This is often called key-query-value self-attention: I have a matrix **Q**, *d* by *d*, which maps **x**ᵢ — a vector of dimensionality *d* — to another vector of dimensionality *d*, and that's going to be a query vector — it takes an **x**ᵢ and rotates it, shuffles it around, stretches it, squishes it, makes it different, and now it's a query.

**[15:36]** And with a different learnable parameter **K** — another matrix — I'm going to come up with my keys, and with a different learnable parameter **V**, I'm going to come up with my values. I'm taking each of the non-contextual word embeddings, each of these **x**ᵢ's, and transforming each of them to come up with my query for that word, my key for that word, and my value for that word — every word is doing each of these roles. Next I'm going to compute all pairs of similarities between the keys and queries. In the toy example we saw, I was computing the similarity between a single query, for the word "learned," and all of the keys for the entire sentence; in this context I'm computing all pairs of similarities between all keys and all

**[16:21]** queries, because I want to represent all of these sums. I've got this dot product — I'm just going to take the dot product between these two vectors: **q**ᵢ, the query for word *i*, dotted with **k**ⱼ, the key for word *j*, and I get this score, which is a real value — might be very large negative, might be zero, might be very large and positive — and that's like, how much should I look at *j* in this lookup table. And then I do the softmax: the actual weight that I'm going to look at *j* from *i* is the softmax of this over all of the possible indices — it's the affinity between *i* and *j*, normalized by the affinity between *i* and all of the possible *j′* in the

**[17:08]** sequence. And then my output is the weighted sum of values: my output for word *i* — maybe *i* is 1, for "Zuko" — is represented as the sum, over all *j* — "Zuko," "made," "his," "uncle," "tea" — of the weight α, subscript *ij*, looking from *i* to *j*, times the value vector for word *j*. *[Student question:]* "Can **w**ᵢ be either a symbol in the vocabulary, or a one-hot vector?" You could think of it as a one-hot vector, in dimensionality equal to the size of the vocabulary. In

**[17:55]** the matrix **E**, that's *d* by |V|, where |V| is the size of the vocabulary, so when I do **E** multiplied by **w**ᵢ, that's taking **E**, which is *d* by |V|, multiplying it by **w**ᵢ, which is |V|-dimensional, and returning a vector of dimensionality *d*. *[Student, on the shape of the one-hot matrix across a sequence:]* it's a matrix with a column for every word in the sentence, and each column has length |V| — or, if you put the sequence-length index first, you might think of it as having a row for each word — similarly, it's *n*, the sequence length, by |V|, the vocabulary size, and that gets

**[18:41]** mapped to something that's sequence-length by *d*. *[Student question:]* "Why do we learn two different matrices **Q** and **K**, when **q**ᵢᵀ**k**ⱼ is really just one matrix in the middle?" That's a great question — it ends up being because this will be a low-rank approximation to that matrix; it's for computational-efficiency reasons, although it also feels kind of nice in the presentation. What we'll end up doing is having a low-rank approximation to **QK**ᵀ, and yes, we actually do it like this — good question. *[Student, following up:]*

**[19:27]** "Does e, subscript *ii* — the query of a word dotted with its own key — look like anything in particular, like the identity?" Let me repeat the question: does e, subscript *ii*, for *j* equal to *i* — looking at itself — look like anything in particular, like the identity? That's the question. It's unclear, actually — this question of whether you should look at yourself when representing yourself. Well, it's going to be encoded by the matrices **Q** and **K**: if **Q** and **K** were both the identity, then this would be dotting yourself with yourself, which is going to be high on average — you're pointing in the same direction as yourself — but it

**[20:14]** could be that **Q**​**x**ᵢ and **K**​**x**ᵢ end up sort of arbitrarily different from each other, because **Q** could be the identity and **K** could map you to the negative of yourself, for example, so that you don't look at yourself. This is all learned in practice, so the model can decide, by learning, whether you should be looking at yourself or not — that's some of the flexibility that parameterizing **Q** and **K** gives you, which wouldn't be there if I just used **x**ᵢ's everywhere in this equation. I'm going to try to move on, I'm afraid, because there's a lot to get through, but we'll keep talking about self-attention, and as more questions come up I can also potentially return to it.

**[21:01]** So this is our basic building block, but there are a bunch of barriers to using it as a replacement for our LSTMs, and so for this portion of the lecture we're going to talk about the minimal components we need in order to use self-attention as this very fundamental building block — we can't use it as it stands, as I've presented it, because there are a couple of things we need to solve or fix. One of them is that there's no notion of sequence order in self-attention. What does this mean? If I have a sentence — I'm going to move over to the whiteboard briefly, and hopefully I'll write quite large —

**[21:46]** if I have a sentence like "Zuko made his uncle," and, let's say, "his uncle made Zuko" — if I were to embed each of these words using its embedding matrix, the embedding matrix isn't dependent on the index of the word: this is word index one, two, three, four, versus now "his" is over here, and "uncle" — and so when I compute the self-attention — there's a lot more on this in the lecture notes, which goes through a full example — the actual self-attention operation will

**[22:32]** give you exactly the same representations for the sequence "Zuko made his uncle" as for the sequence "his uncle made Zuko," and that's bad, because they're sentences that mean different things. It's this idea that self-attention is an operation on sets — you have a set of vectors that you're going to perform self-attention on, and nowhere does the exact position of the words come into play directly. So we're going to encode the position of words through the keys, queries, and values that we have. Consider now representing each sequence index — our sequences go from 1 to *n* — as a vector; don't worry so far about how it's being made, but you can imagine

**[23:17]** representing the number "position one," "position two," "position three" as a vector in dimensionality *d*, just like we're representing our keys, queries, and values — these are position vectors, **p**ᵢ. If you wanted to incorporate the information represented by these positions into our self-attention, you could just add these vectors, these **p**ᵢ vectors, to the inputs — so if I have this **x**ᵢ embedding of a word, which is the word at position *i*, but really just represents, say, the word "Zuko" is here, now I can say it's the word "Zuko" and it's at position five, because

**[24:02]** this vector represents position five. So how do we do this — and we might only have to do this once, at the very input to the network, and then that's sufficient; we don't have to do it at every layer, because it's known from the input. One way people have done this is with these sinusoidal position representations — this looks a little bit like this, where you have a vector **p**ᵢ, in dimensionality *d*, and for each one of the dimensions, you take the value *i*, modify it by some constant, and pass it to the sine or cosine function, and you get these

**[24:48]** values that vary according to differing periods, depending on the dimension. I've got a representation of a matrix where *d* is the vertical dimension and *n* is the horizontal, and you can see that as you walk along, you see the period of the sine function going up and down, and each of the *d* dimensions has a different period, and together you can represent a bunch of different position indices. This gives the intuition that maybe the absolute position of a word isn't as important — you've got the periodicity of the sines and cosines, and maybe that allows you to extrapolate to longer sequences, but in practice that doesn't work. This is an early

**[25:33]** notion that's still sometimes used for how to represent position in Transformers and self-attention networks in general. So that's one idea — you might think it's a little bit complicated, a little bit unintuitive. Here's something that feels a little bit more deep-learning: we're just going to say, I've got a maximum sequence length of *n*, and I'm going to learn a matrix that's dimensionality *d* by *n*, and that's going to represent my positions — I'm going to learn it as a parameter, just like every other parameter. What do the entries mean? I have no idea, but it represents position. You just add this matrix to the **x**ᵢ's, your input

**[26:19]** embeddings, and it learns to fit to the data — so whatever representation of position that's linear, index-based, that you want, you can learn. The cons are that you definitely now can't represent anything longer than *n* words long — no sequence longer than *n* you can handle, because you only learned a matrix of that many positions, and in practice you'll get a model error if you pass a self-attention model something longer than length *n* — it will just crash and say, "I can't do this." This is what most systems nowadays use — there are more flexible representations of position, including a couple in the lecture notes you might

**[27:05]** want to look at, like relative linear position — words before or after each other, but not their absolute position. There are also some representations that hearken back to dependency syntax — maybe words that are close in the dependency parse tree should be the things that are close in the self-attention operation. *[Student question:]* "In practice, do we typically just make *n* large enough that we don't run into the issue of the input being longer than that?" So the question is, do we just make *n* long enough that we don't run into the problem of a text longer than *n*? No — in practice it's actually quite a problem, even today, even in the largest, biggest language

**[27:51]** models — "can I fit this prompt into ChatGPT" or whatever you might see on Twitter, these continue to be issues, and part of it is because the self-attention operation — we'll get into this later in the lecture — is quadratic complexity in the sequence length, so you're going to spend an *n*-squared memory budget in order to make sequence lengths longer. In practice, this might be, on a large model, say 4,000 or so — *n* is 4,000, so you can fit 4,000 words, which feels like a lot, but it's not going to fit a novel, it's not going to fit a Wikipedia page. There are models that do longer sequences for sure, and we'll talk a bit about it, but no, this actually is an issue.

**[28:48]** *[Student question:]* "How do you know that the **P** matrix you've learned is representing position, as opposed to anything else?" The reason is: it's the only thing that correlates with position. When I add this **P** matrix to my **X** matrix, the word embeddings, I'm adding them together, and the words that show up at each index will vary depending on what word actually showed up there in the example, but the **P** matrix never differs — it's always exactly the same at every index, and so it's the only thing in the data that correlates with position. You're implicitly learning that this vector, at index one, is always at index one, for every example, for every gradient update, and nothing else co-occurs like that. What you end up learning, I don't

**[29:34]** know — unclear — but it definitely allows the model to know, "this word is at this index." *[Student question:]* "Just quickly, in this case, when this is quadratic in the sequence — is that a sequence of words?" Yeah, think of it as a sequence of words — sometimes there'll be pieces that are smaller than words, which we'll go into in the next lecture, but think of this as a sequence of words, not necessarily just a sentence —

**[30:19]** the attention is based on word to word, but it could be for an entire paragraph or an entire document or something like that. Okay, cool, I'm going to move on. We have another problem: based on the presentation of self-attention we've done, there are really no nonlinearities for deep-learning "magic" — we're just computing weighted averages of stuff. If I apply self-attention, and then apply self-attention again, and again and again, you should — look at the next lecture notes if you're interested in this, it's actually quite cool — but what you end up doing is just re-averaging value vectors together, and it ends up looking like one big self-attention. There's an easy fix to this, if you want the

**[31:06]** traditional deep-learning magic: you can just add a feed-forward network to post-process each output vector. I've got a word here that's the output of self-attention, and I'm going to pass it through — in this case I'm calling it a multi-layer perceptron, MLP — a vector in dimensionality *d*, taking in as input a vector in dimensionality *d*, and you do the usual multi-layer-perceptron thing, where you multiply by a matrix, pass it to a nonlinearity, multiply by another matrix. What this looks like in self-attention: I've got the sentence "the chef who … food," I've got my embedding for it, I pass it through this whole big self-attention block, which looks at the whole sequence and incorporates context, and then I pass each one individually

**[31:52]** through a feed-forward layer — this embedding, the output of self-attention for the word "the," is passed independently through a multi-layer perceptron. You can think of it as combining, or processing, the result of attention. There's a number of reasons we do this — one of them is that you can actually stack a ton of computation into these feed-forward networks very efficiently, very parallelizable, very good for GPUs, but this is what's done in practice: you do self-attention, and then you pass it through a position-wise feed-forward layer — every word is processed independently by this feed-forward network to process the result.

**[32:39]** That's adding our classical deep-learning nonlinearities for self-attention — an easy fix for the no-nonlinearities problem. Then we have a last issue before we have our final minimal self-attention building block, with which we can replace RNNs: when I've been writing out all of these examples of self-attention, you can look at the entire sequence, but in practice, for some tasks — such as machine translation or language modeling, whenever you want to define a probability distribution over a sequence — you can't cheat and look at the future. At every time step I could define the set of keys and

**[33:25]** queries and values to only include past words, but this is inefficient — bear with me — it's inefficient because you can't parallelize it well. So instead we compute the entire *n*-by-*n* matrix, just like I showed in the slide discussing self-attention, and then we mask out words in the future: if this score e, subscript *ij* — and I computed e, subscript *ij*, for all *n*-by-*n* pairs of words — is equal to whatever it was before if the word you're looking at, at index *j*, is at an index less than or equal to where you are, index *i*, and it's equal to negative infinity otherwise, if it's in the future. When you softmax the e, subscript *ij*, negative infinity gets mapped to zero,

**[34:10]** so now my attention weight is zero — my weighted average has zero weight on the future, so I can't look at it. What does this look like: so, in order to encode "the," "chef," "who," and maybe the start symbol there, I can look at these words — that's all pairs of words — and then I gray out, I negative-infinity-out, the words I can't look at: encoding the start symbol, I can only look at the start symbol; encoding "the," I can look at the start symbol and "the"; encoding "chef," I can look at "start," "the," "chef" — but I can't look at "who." And so,

**[34:56]** with this representation of "chef," which is only looking at "start," "the," "chef," I can define a probability distribution using this vector that allows me to predict "who," without having cheated by already looking ahead and seeing that "who" is the next word. *[Student question:]* "For using this in decoders, do we do this for both the encoding layer and the decoding layer, or only the encoding layer — are we allowing ourselves to look forward?" The question is, we're using this in a decoder — do we also use it in the encoder? This is the distinction between a bidirectional LSTM and a unidirectional LSTM: wherever you don't need this constraint, you probably don't use it. If you're using an encoder, on the source sentence of your machine translation

**[35:41]** problem, you probably don't do this masking, because it's probably good to let everything look at each other; and whenever you do need to use it, because you have this autoregressive probability — probability of word one, probability of word two given word one, word three given words two and one — then you would use this. So traditionally, yes, in decoders you will use it; in encoders you will not. *[Student question:]* "My question is a bit philosophical, about how humans actually generate sentences by having some notion of the probability of future words before they say — before they choose the words that they are actually speaking or writing or generating." Good

**[36:27]** question — the question is, isn't looking ahead a little bit, and predicting or getting an idea of the words you might say in the future, sort of how humans generate language, instead of the strict constraint of not seeing into the future? Is that what you're [asking]? So, trying to plan ahead to see what I should do is definitely an interesting idea, but when I am training the network, if I'm teaching it to try to predict the next word, and I give it the answer, it's not going to learn anything useful. In practice, when I'm generating text, maybe it would be a good idea to make some guesses far into the future, or have a high-level plan or something, but in training the network I can't encode that intuition about how humans

**[37:13]** generate sequences of language by just giving it the answer of the future directly — at least, because then it's just too easy, there's nothing to learn. But there might be interesting ideas about giving the network a hint as to what kind of thing could come next, for example, but that's out of scope for this. *[Student question:]* "I understand why we want to mask the future for stuff like language models, but how does it apply to machine translation — why would we use it there?" So in machine translation — I'm going to come over to this board, and hopefully get a better marker — nice — in machine translation, I have a sentence like "I like pizza," and I want to be able to

**[38:01]** translate it, "j'aime la pizza." When I'm looking at "I like pizza," I get this as the input, and so I want self-attention without masking, because I want "I" to look at "like," and "I" to look at "pizza," and "like" to look at "pizza" — I want it all. And then, when I'm generating this — if my tokens are like "j'," "aime," "la," "pizza" — I want, in encoding this word, to be able to look only at myself, and — we'll talk about encoder-decoder architectures later in the lecture — but I want to be able to look at

**[38:47]** myself, none of the future, and all of the [past]. So what I'm talking about right now, in this masking case, is masking out, with negative infinity, all of these words, so that the attention score from [this word] to everything else [in the future] should be negative infinity. Does that answer your question? Great, okay, let's move ahead. So that was our last big building-block issue with self-attention — this is what I would call, and this is my personal opinion, a minimal self-attention building block: you have self-attention, the basis of the method, here in red, and maybe we had the inputs to the sequence here, and then you embed

**[39:32]** it with that embedding matrix **E**, and then you add position embeddings — then these three arrows represent using the key, the value, and the query, stylized there — this is often how you see these diagrams. You pass it to self-attention, with the position representation, which specifies the sequence order, because otherwise you'd have no idea what order the words showed up in. The nonlinearities in the teal feed-forward network provide that squashing and deep-learning expressivity, and then you have masking, in order to have parallelizable operations that don't look at the future. So this is our minimal architecture, and then, up at the top

**[40:19]** above, you repeat this self-attention-and-feed-forward block many times — self-attention, feed forward, self-attention, feed forward, self-attention, feed forward — that's what I'm calling this block — and then maybe at the end of it you predict something; I don't know, we haven't really talked about that, but you have these representations and then you predict the next word, or predict the sentiment, or predict whatever — so this is a self-attention architecture. We're going to move on to the Transformer next — any questions? *[Student, clarifying the masking direction:]* other way around — we will use masking for decoders, where I want to decode out a sequence, where I have an informational constraint that, to represent this word properly, I cannot have the information

**[41:05]** of the future. Okay, great, now let's talk about Transformers. What I've pitched to you is what I call a minimal self-attention architecture, and I quite like pitching it that way, but really no one uses the architecture that was just up on the slide — it doesn't work quite as well as it could, and there's a bunch of important details we'll talk about now that go into the Transformer. But what I would hope you take away is that the Transformer architecture, as I'll present it now, is not necessarily the

**[41:51]** end point of our search for better and better ways of representing language, even though it's now ubiquitous, and has been for a couple of years. So think about these ideas of the problems of using self-attention, and maybe ways of fixing some of the issues, as Transformers [continue to evolve]. A Transformer decoder is how we'll build systems like language models — we've discussed this, it's like our decoder with our self-attention-only minimal architecture, with a couple of extra components, some of which I've grayed out here, that we'll go over one by one. The first thing that's actually different is that we'll replace our self-attention-with-masking with masked multi-head self-attention — this ends up being crucial, it's probably the most important

**[42:38]** distinction between the Transformer and this minimal architecture I've presented. Let's come back to our toy example of attention, where we've been trying to represent the word "learned" in the context of the sequence "I went to Stanford, CS224N, and learned" — and I was giving these teal bars to say, maybe intuitively, you look at various things to build up your representation of "learned." But really there are varying ways in which I want to look back at the sequence, to see varying aspects of information I want to incorporate into my representation — so maybe in one way I want to look at "Stanford," "CS224N," because it's

**[43:25]** like — you learn different stuff at Stanford, CS224N, than you do in other courses or other universities or whatever — so maybe I want to look here for this reason, and in another sense I actually want to look at the word "learned," and I want to look at "I" — "I went and learned" — maybe syntactically relevant words. It's for very different reasons that I might want to look at different things in the sequence, and so trying to average it all out with a single operation of self-attention ends up being maybe somewhat too difficult, in a way we'll make precise in Assignment 5 — we'll do a little bit more math. Any questions about this intuition?

**[44:12]** *[Student question:]* "Should it be an application of attention just as presented — you independently define the keys, define the queries, define the values, and just do attention once, and then do it again, with different parameters, being able to look at different things?" [Instructor:] we do that. So the question is, if we have two separate sets of weights, trying to learn to do this and to do that, how do we ensure they learn different things? We do not ensure that — we hope they learn different things, and in practice they do, although not perfectly, so it ends up being the case that you have some redundancy, and you can cut out some of these, but that's out

**[44:58]** of scope for this — we hope, just like we hope that different dimensions in our feed-forward layers will learn different things because of lack of symmetry, that the heads will start to specialize, and they'll specialize even more. Okay, so, in order to discuss multi-head self-attention, we really need to talk about the matrices — how we're going to implement this efficiently on GPUs — we're going to talk about the sequence-stacked form of attention. We've been talking about each word individually as a vector in dimensionality *d*, but really we're going to work on these as big stacked matrices — I take all of my word embeddings **x**₁ to **x**ₙ and stack them together, and now I

**[45:44]** have a big matrix, **X**, *n* by *d*. Now, with my matrices **K**, **Q**, and **V**, I can just multiply them on the side of **X** — **X** is *n* by *d*, **K** is *d* by *d*, so *n* by *d* times *d* by *d* gives you *n* by *d* again, so I can compute a big matrix multiply on my whole sequence, to multiply each of the words with my key, query, and value matrices very efficiently — this is the vectorization idea, I don't want to for-loop over the sequence, I represent the sequence as a big matrix, and do one big matrix multiply. Then the output is defined by this

**[46:29]** somewhat inscrutable bit of math, which I'm going to go over visually. First we're going to take the key-query dot products in one matrix: we've got **XQ**, which is *n* by *d*, and **XK**ᵀ, which is *d* by *n*, so *n* by *d* times *d* by *n* — this is computing all of the e, subscript *ij*'s, these scores for self-attention, all pairs of attention scores computed in one big matrix multiply — this big matrix here. Next I use the softmax — I softmax this over the second,

**[47:15]** the *n*, dimension — and I get my normalized scores, and then I multiply with **XV** — this is an *n*-by-*n* matrix multiplied by an *n*-by-*d* matrix, and what do I get? This is just doing the weighted average — one big weighted-average contribution on the whole matrix, giving me my whole self-attention output, *n* by *d*. I've just restated the self-attention operations identically, but computed in terms of matrices, so you could do this efficiently on a GPU. So, multi-headed attention — it's going to be important to compute this in terms of matrices, and this is

**[48:00]** going to give us the ability to look in multiple places at once, for different reasons. First, self-attention looks where this dot product — **x**ᵢ, the **Q** matrix, the **K** matrix — is high, but maybe we want to look in different places for different reasons, so we actually define multiple query, key, and value matrices. I'm going to have a bunch of heads — I'm going to have *h* self-attention heads — and for each head I'm going to define an independent query, key, and value matrix, and its shape is going to map from the model dimensionality to the model dimensionality over *h*: each one of these is projecting down to a lower-dimensional space, this is

**[48:45]** for computational efficiency, and I'll apply self-attention independently for each output. This equation here is identical to the one we saw for single-headed self-attention, except we've got the ℓ index everywhere — I've got this lower-dimensional thing, I'm mapping to a lower-dimensional space, and I have my lower-dimensional value vector — my output is in dimensionality *d*/*h*, but really you're doing exactly the same kind of operation, just *h* different times, and then you combine the outputs: I've looked in different places with the different key, query, and value matrices, and then I get each of their outputs and concatenate them together —

**[49:30]** each one is dimensionality *d*/*h*, I concatenate them together, and then mix them together with a final linear transformation, so each head gets to look at different things and construct their value vectors differently, and then I combine the result altogether at once. Let's go through this visually, because it's at least helpful for me — it's actually not more costly to do this than it is to compute a single head of self-attention, and we'll see through the pictures: in single-headed self-attention we computed **XQ**; in multi-headed self-attention we'll also compute **XQ** the same way — **XQ** is *n* by *d*,

**[50:19]** and then we can reshape it into *n* — sequence length — times the number of heads, times the model dimensionality over the number of heads. I've just reshaped it to say, now I've got a big three-axis tensor: the first axis is the sequence length, the second is the number of heads, the third is this reduced model dimensionality — and that costs nothing. I do the same thing for **X** and **V**, and then I transpose, so I've got the head axis as the first axis, and now I can compute all my other operations with the head axis kind of like a batch. What does this look like in practice? Instead of one big **XQ** matrix that's model dimensionality *d*,

**[51:06]** I've got, in this case, three **XQ** matrices of model dimensionality *d* over 3, *d* over 3, *d* over 3 — same thing with the key matrix. Everything looks almost identical, it's just a reshaping of the tensors, and now, at the output of this, I've got three sets of attention scores, just by doing this reshape — and the cost is that each of my attention heads has only a *d*/*h*-dimensional vector to work with, instead of a *d*-dimensional vector. I get the output — three sets of pairs of scores — I compute the softmax independently for each of the three, and then I have three value matrices as well, each of them lower-dimensional, and then finally,

**[51:53]** I get my three different output vectors, and I have a final linear transformation to mush them together, and I get an output. In summary, what this allows you to do is exactly what I gave in the toy example: each of these heads can look at different parts of a sequence for different reasons — and this is at a given block, all of these attention heads are for a given Transformer block, and a next block would also have — *[Student question:]* "are all of these for a given block?" — and we'll talk about a block again, but this block was this pair of self-attention and feed-forward network, so you do self-attention, feed forward, that's one block, another block is another self-attention, another feed forward. *[Student question:]* "Are the parameters shared between the blocks or not?"

**[52:39]** Generally they are not shared — you'll have independent parameters at every block, although there are some exceptions. *[Student question:]* "Is it typically the case that you have the same number of heads at each block, or do you vary the number of heads across blocks?" You definitely could vary it — people haven't found reason to. The simplest thing is to just have it be the same everywhere, which is what people have done — I haven't yet found a good reason to vary it, but it could be interesting. It's definitely the case that, after training

**[53:24]** these networks, you can actually totally zero out and remove some of the attention heads, and I'd be curious to know if you could remove more or less depending on the layer index, which might say we should just have fewer — but again, it's not actually more expensive to have a bunch, so people tend to instead set the number of heads so that you have a reasonable number of dimensions per head, given the total model dimensionality *d* you want — for example, I might want at least 64 dimensions per head, which, if *d* is, say, 128, tells me how many heads I'm going to have, roughly — so people tend to scale the number of heads up with the model dimensionality.

**[54:13]** *[Student question:]* "By slicing it into different columns, you're reducing the rank of the final matrix, right — does that not have any effect on the results?" So the question is, by having these reduced **XQ** and **XK** matrices — this is a very low-rank approximation, this little sliver defining this whole big matrix, it's very low rank — is that not bad in practice? No — again, it's part of the reason we limit the number of heads depending on the model dimensionality, because you want, intuitively, at least some number of dimensions — 64 is sometimes done, 128, something like that — but if you're not giving each head too much to do, and it's got

**[54:58]** a simple job, you've got a lot of heads, it ends up being okay. At the very least, all we really know is that empirically it's way better to have more heads than just one. *[Student question:]* "Have there been studies to see if the information one of the sets of attention scores learns is consistent, and related to each other?" So the question is, have there been studies to see if there's consistent information encoded by the attention heads — yes, actually, there's been quite a lot of study and interpretability analysis of these models, trying to

**[55:45]** figure out what mechanistic roles each of these heads takes on, and there's quite a bit of exciting results around some attention heads learning to pick out syntactic dependencies, or maybe doing a sort of global averaging of context. The question is quite nuanced though, because in a deep network it's unclear — we should talk about this more offline — but it's unclear if you look at a word ten layers deep in a network, what you're really looking at, because it's already incorporated context from everyone else, and it's a little bit unclear — active area of research — but I think I should move on now to keep discussing Transformers, but if you want to talk more about it, I'm happy to.

**[56:30]** So, another hack that I'm going to toss in here — maybe they wouldn't call it a hack, it's a nice little method to improve things — it's called scaled dot-product attention. One of the issues with this key-query-value self-attention is that when the model dimensionality becomes large, the dot products between vectors — even random vectors — tend to become large, and when that happens, the inputs to the softmax function can be very large, making the gradients small. Intuitively, if you have two random vectors in model dimensionality *d*, and you dot-product them together, as *d* grows, their dot product grows, in expectation, to be very large, and you sort of want to start out with everyone's attention being very uniform,

**[57:16]** very flat, looking everywhere, but if some dot products are very large, then learning will be inhibited. So what you end up doing is, for each of your heads, you divide all the scores by a constant that's determined by the model dimensionality, so that as the vectors grow very large, their dot products don't — at least at initialization time. This is a nice little — important, but maybe not — well, it's important to know — and that's called scaled dot-product attention. From here on out we'll just assume we do this — it's quite easy to implement, you just do a little division in all of your

**[58:02]** computations. So now, in the Transformer decoder, we've got a couple of other things I have faded out here — we have two big optimization tricks, or optimization methods, I should say, because these are quite important: residual connections and layer normalization. In Transformer diagrams that you see around the web, they're often written together as this "Add & Norm" box, and in practice, in the Transformer decoder, I'll apply masked multi-head attention, and then do this "Add & Norm," then a feed-forward application, then another "Add & Norm." Let's go

**[58:48]** over these two individual components — the first is residual connections. I think we've talked about residual connections before, but it's worth doing again — it's really a good trick to help models train better. Just to recap: instead of having a layer *i* − 1 that you pass through a thing — maybe it's self-attention, maybe it's a feed-forward network — to get layer *i*, I'm going to add the result of layer *i* to its input, so I'm just going to compute the layer and add in the input to the layer, so that I only have to learn the residual from the previous layer — I've got this

**[59:33]** connection here, often drawn going around — and you should think that the gradient flows really well through the residual connection: if I've got vanishing or exploding gradients through this layer, I can at least learn everything behind it, because I've got this residual connection where the gradient is one, because it's the identity. This is really nice, and it also maybe means that, at least at initialization, everything looks a little bit like the identity function, because if the contribution of the layer is somewhat small — because all of your weights are small — and I have the addition from the input, maybe the whole thing looks a little bit like the identity, which might be a good place to start,

**[1:00:20]** and there are really nice visualizations — I just love this visualization — this is your loss landscape: you're doing gradient descent, trying to traverse the mountains of the loss landscape, this is the parameter space, and down is better in your loss function, and it's really hard, you get stuck in some local optimum and can't find your way out — and then, with residual connections, I mean, come on, you just walk down. I mean, it's not actually really how it works all the time, but I really love this, it's great. Okay, we've seen residual connections, we should move on to layer normalization. Layer norm is another thing to

**[1:01:05]** help your model train faster, and the intuitions around layer normalization, and the empiricism of it working very well, maybe aren't perfectly connected, but you should imagine that we want — say, this variation within each layer, things can get very big, things can get very small, that's not actually informative, because of variations between — maybe the gradients, or I've got sort of weird things going on in my layers that I can't totally control, I haven't been able to make everything behave nicely, where everything stays roughly the same norm — maybe some things explode, maybe

**[1:01:51]** some things shrink — and I want to cut down on that uninformative variation between layers. So I'm going to let **x** be an individual word vector in the model, dimensionality *d* — this is at a single index, one vector — and what I'm going to try to do is normalize it, in the sense that it's got a bunch of variation, and I'm going to cut that out — normalize it to unit mean and standard deviation. I'm going to estimate the mean, here, across all of the dimensions in the vector — *j* equals 1 to the model dimensionality — I'm going to sum up the value, so I've got this one big word vector, and I sum up all the values, divided by *d* — that's

**[1:02:39]** the mean. I'm going to have my estimate of the standard deviation — again, these should say "estimates," this is my simple estimate of the standard deviation of the values within this one vector — and then, possibly, I can have learned parameters to scale back out, multiplicatively and additively — that's optional. We're going to compute this standardization: take my vector **x**, subtract out the mean, divide by the standard deviation plus this ε constant — if there's not a lot of variation, I don't want things to explode, so I have this ε there, close to zero. So this part, **x** minus μ, over the square root of σ

**[1:03:25]** plus ε, is saying, take all the variation, and normalize it to unit mean and standard deviation, and then maybe I want to scale it, stretch it back out, and add an offset β that I've learned — although in practice, and I discuss this in the lecture notes, this part maybe isn't actually that important. So, layer normalization — you can think of it as: when I get the output of layer normalization, it's going to look nice, and look similar, to the next layer, independent of what's gone on, because it's going to be unit mean and standard deviation, so maybe that makes for a better thing to learn off of for the next layer. Any questions for residual or

**[1:04:12]** layer norm? *[Student question:]* "When you subtract the scalar μ from the vector **x**ᵢ, do you broadcast μ to dimensionality *d* and remove μ from all of it?" Yeah, good point, thank you — that was unclear — yes. *[Student, repeating:]* "In the fourth bullet point, when you're calculating the mean, is it divided by *d*?" I think it is divided by *d* — yes, this is the average deviation from the mean of all of the

**[1:04:59]** [dimensions]. *[Student question:]* "Is this normalized based on the statistics [per word, or shared across the sequence]?" So the question is, if I have five words in the sequence, do I normalize by aggregating the statistics to estimate μ and σ across all five words — share their statistics — or do it independently for each word? This is a great question, which I think in all the papers that discuss Transformers is under-specified: you do not share across the five words, which is somewhat confusing to me, so each of the five words is done completely independently. You could have shared across the five words, and said your estimate of the statistics is based on all five, but you do not. *[Student question:]*

**[1:05:46]** "I can't pretend I understand totally why — for example, per batch, of the same position?" So, a similar question — the question is, if you have a batch of sequences, just like we're doing batch-based training, do you, for a single word, now — we don't share across a sequence index for the statistics — do you share across the batch? And the answer is: no, you also do not share across the batch. In fact, layer normalization was invented as a replacement for batch normalization, which did just that, and the issue with batch normalization is that your forward pass depends, in a way you don't like, on

**[1:06:32]** examples that should be unrelated to your example, so you don't share statistics across the batch. Okay, cool, so now we have our full Transformer decoder, and we have our blocks: in this slightly grayed-out thing that says "repeat for a number of decoder blocks," each block consists of — I pass it through self-attention, and then my Add & Norm — I've got this residual connection that goes around, and the layer normalization there — and then a feed-forward layer, and then another Add & Norm — and that set of four operations I apply

**[1:07:19]** for some number of times, the number of blocks — that whole thing is called a single block, and that's it, that's the Transformer decoder as it is. So that's a whole architecture right there — we've solved things like needing to represent position, we've solved things like not being able to look into the future, we've solved a lot of different optimization problems. *[Student question:]* [about masking and scaling in multi-head attention:] Masked multi-head attention, yeah, with the dot-product scaling, with the square root of *d*/*h* as well, yeah. *[Student question:]*

**[1:08:05]** "How do these models handle variable-length inputs?" So if you have — the input to the GPU forward pass is going to be a constant length, so you're going to pad to a constant length, and in order to not look at the future — the stuff that's happening in the future — you can mask out the pad tokens, just like the masking we showed for not looking at the future in general: you can just set all of the attention weights, or the scores, to negative infinity for all of the pad tokens.

**[1:08:52]** *[Student question:]* "Do you set this length to always be the maximum length?" You know, yes, often, although you can save computation by setting it to something smaller — the math all still works out, you just have to code it properly, so you can handle — you set everything, instead of *n*, you set it all to five, if everything is shorter than, say, five, and you save a lot of computation, and all of the self-attention operations just work. *[Student question:]* "There's one hidden layer in the feed forward?" Yeah — okay, I should move on, I've got a couple more things and not very much time — but I'll be here after class as well. So, in the encoder — the Transformer encoder is almost identical,

**[1:09:39]** but again, we want bidirectional context, so we just don't do the masking — I've got no masking in my multi-head attention here, and so it's that easy to make the model bidirectional. That's called the Transformer encoder — it's almost identical, but no masking. And then, finally, we've got the Transformer encoder-decoder, which is actually how the Transformer was originally presented, in the paper "Attention Is All You Need." This is when we want a bidirectional network — here's the encoder, it takes in, say, my source sentence for machine translation, its multi-head attention is not masked, and I have a decoder to decode out my sentence — but you'll see this is slightly more complicated: I have my

**[1:10:25]** masked multi-head self-attention, just like I had before in my decoder, but now I have an extra operation called cross-attention, where I'm going to use my decoder vectors as my queries, and take the output of the encoder as my keys and values, so now, for every word in the decoder, I'm looking at all the possible words in the output of all the blocks of the encoder. *[Student question:]* "How do we get a key and a value separated out from the output, when we collapsed those into a single output?" So the question is, how do you get the keys

**[1:11:11]** and values and queries out of this sort of single collapsed output — didn't we collapse the keys and values into a single output? So the output — we capture those. The question is how you get the keys and values and queries out of this collapsed output: remember, the output for each word is just this weighted average of the value vectors for the previous words, and then, from that output, for the next layer, we apply a new key, query, and value transformation to each of them, for the next layer of self-attention — so it's not actually — you apply the key matrix, the query matrix, to the output of whatever came before it. In a little bit of math:

**[1:11:57]** we have these vectors **h**₁ through **h**ₙ, which I'm going to call the output of the encoder, and then I've got vectors that are the output of the decoder, these **z**'s, the output of the decoder, and then I simply define my keys and my values from the encoder vectors, these **h**'s — I take the **h**'s, apply a key matrix and a value matrix — and then I define the queries from my decoder, so my queries, my **z**'s here — this is why two of the arrows come from the encoder and one arrow comes from the decoder — I get my queries, my keys and values from the encoder. So that's it — I've got a couple of

**[1:12:45]** minutes, I want to discuss some of the results of Transformers, and I'm happy to answer more questions about Transformers after class. So, really, the original results of Transformers had this big pitch: look, you can do way more computation because of parallelization — they got great results in machine translation, Transformers doing quite well, although not astoundingly better than existing machine translation systems, but they were significantly more efficient to train, because you don't have this parallelization problem — you could compute on much more data, much faster, and make use of faster GPUs much more.

**[1:13:31]** After that there were things like document generation, where you had the old standard of sequence-to-sequence models with LSTMs, and eventually everything became Transformers all the way down. Transformers also enabled this revolution into pre-training, which we'll go over in the lecture next class, and the efficiency, the parallelizability, allows you to compute on tons and tons of data, and so, after a certain point, on standard large benchmarks, everything became Transformer-based — this ability to make use of lots and lots of data, lots and lots of compute, put Transformers head and shoulders above LSTMs in almost every modern advancement in natural language processing.

**[1:14:19]** There are many drawbacks and variants to Transformers — the clearest one people have tried to work on quite a bit is this quadratic compute problem: this all-pairs-of-interactions means our total computation for each block grows quadratically with the sequence length, and, in a student's question, we heard that as the sequence length becomes long — if I want to process a whole Wikipedia article, a whole novel — that becomes quite unfeasible, and actually that's a step backwards in some sense, because for recurrent neural networks it only grew linearly with the sequence length. Other things people have tried to work on are better position representations, because the absolute index of a word is not really the best way to represent

**[1:15:05]** its position in a sequence. Just to give you an intuition of quadratic-in-sequence-length: remember we had this big matrix multiply that resulted in a matrix of *n* by *n*, and computing this is a big cost — it costs a lot of memory. There's been work — oh yeah, if you think of the model dimensionality as, like, a thousand — although today it gets much larger — then for a short sequence, *n* roughly 30, computing *n*-squared times *d*, 30 isn't so bad, but if you had something like 50,000, then *n*-squared becomes huge, totally infeasible, so people have tried to map things down to a lower-dimensional space to get rid of

**[1:15:51]** the quadratic computation. But in practice, as people have gone to things like GPT-3, ChatGPT, most of the computation doesn't show up in the self-attention, so people are wondering whether it's even necessary to get rid of the quadratic-attention constraint — it's an open area of research whether this is necessary. Finally, there have been a ton of modifications to the Transformer over the last four- or five-ish years, and it turns out that the original Transformer, plus maybe a couple of modifications, is pretty much the best thing there is. There have been a couple of things that end up being important — changing the nonlinearities in the feed-forward network ends up

**[1:16:37]** being important — but it's had lasting power so far, and I think it's right for people to come through and think about how to improve it in various ways. So, pre-training is on Tuesday, good luck on Assignment 4, and then we'll have the project proposal documents out tonight for you to talk about.
